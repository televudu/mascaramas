---
import BaseLayout from '../layouts/BaseLayout.astro';
import WorkCatalog from '../components/WorkCatalog.astro';
import { getCatalogData } from '../lib/catalog';

const { projects, information } = await getCatalogData();
---

<BaseLayout title="mascaramas" bodyClass="catalog-page">
  <WorkCatalog projects={projects} information={information} />
</BaseLayout>
